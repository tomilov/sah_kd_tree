skt_add_tests(
    WORKING_DIRECTORY
        "${CMAKE_SOURCE_DIR}/data/scenes"
    LINKS
        "libsah_kd_tree"
        "libscene_loader"
        Qt::Core
        "$<IF:$<BOOL:${BUILD_SHARED_LIBS}>,CUDA::cudart,CUDA::cudart_static>"
)
