project(
    "sah_kd_tree"
    LANGUAGES
        CUDA
)

skt_add_library(
    SOURCES
        "${PROJECT_NAME}_export.h"
        "${PROJECT_NAME}.cuh"
        "${PROJECT_NAME}_inline.cuh"
        "builder.cu"
        "builder.inl.cu"
        "calculate_root_node_bbox.cu"
        "calculate_root_node_bbox.inl.cu"
        "calculate_rope.cu"
        "calculate_rope.inl.cu"
        "calculate_triangle_bbox.cu"
        "calculate_triangle_bbox.inl.cu"
        "check_tree.inl.cu"
        "check_tree.cu"
        "decouple_event_both.inl.cu"
        "decouple_event_both.cu"
        "determine_polygon_side.inl.cu"
        "determine_polygon_side.cu"
        "filter_layer_node_offset.inl.cu"
        "filter_layer_node_offset.cu"
        "find_perfect_split.inl.cu"
        "find_perfect_split.cu"
        "generate_initial_event.inl.cu"
        "generate_initial_event.cu"
        "merge_event.inl.cu"
        "merge_event.cu"
        "populate_leaf_node_triangle_range.inl.cu"
        "populate_leaf_node_triangle_range.cu"
        "populate_node_parent.inl.cu"
        "populate_node_parent.cu"
        "resize_node.inl.cu"
        "resize_node.cu"
        "select_node_best_split.inl.cu"
        "select_node_best_split.cu"
        "separate_splitted_polygon.inl.cu"
        "separate_splitted_polygon.cu"
        "set_node_count.inl.cu"
        "set_node_count.cu"
        "split_node.inl.cu"
        "split_node.cu"
        "split_polygon.inl.cu"
        "split_polygon.cu"
        "update_polygon_node.inl.cu"
        "update_polygon_node.cu"
        "update_splitted_polygon_count.inl.cu"
        "update_splitted_polygon_count.cu"
        "update_splitted_polygon_node.inl.cu"
        "update_splitted_polygon_node.cu"
    PUBLIC_LINKS
        Thrust::Thrust
)

if(NOT CMAKE_UNITY_BUILD_BATCH_SIZE STREQUAL 0)
    set_target_properties(
        "lib${PROJECT_NAME}"
        PROPERTIES
            UNITY_BUILD_BATCH_SIZE 2
    )
endif()

if(SAH_KD_TREE_BUILD_FUZZER)
    target_compile_options(
        "lib${PROJECT_NAME}"
        PRIVATE
            -fsanitize=fuzzer-no-link
    )
    target_link_options(
        "lib${PROJECT_NAME}"
        PRIVATE
            -fsanitize=fuzzer-no-link
    )
    string(REPLACE "." ";" CXX_COMPILER_VERSION_LIST ${CMAKE_CXX_COMPILER_VERSION})
    list(GET CXX_COMPILER_VERSION_LIST 0 CXX_COMPILER_VERSION_MAJOR)
    target_link_directories(
        "lib${PROJECT_NAME}"
        PUBLIC
            "/usr/lib/clang/${CXX_COMPILER_VERSION_MAJOR}/lib/linux/"
    )
    target_link_libraries(
        "lib${PROJECT_NAME}"
        PUBLIC
            "clang_rt.fuzzer_no_main-${CMAKE_SYSTEM_PROCESSOR}"
    )
endif()
