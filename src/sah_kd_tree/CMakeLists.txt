project(
    "sah_kd_tree"
    LANGUAGES
        CUDA
)

skt_add_library(
    SOURCES
        "${PROJECT_NAME}_export.h"
        "${PROJECT_NAME}.cuh"
        "${PROJECT_NAME}_inline.cuh"
        "builder.cu"
        "builder.cu.inl"
        "calculate_root_node_bbox.cu"
        "calculate_root_node_bbox.cu.inl"
        "calculate_rope.cu"
        "calculate_rope.cu.inl"
        "calculate_triangle_bbox.cu"
        "calculate_triangle_bbox.cu.inl"
        "check_tree.cu.inl"
        "check_tree.cu"
        "decouple_event_both.cu.inl"
        "decouple_event_both.cu"
        "determine_polygon_side.cu.inl"
        "determine_polygon_side.cu"
        "filter_layer_node_offset.cu.inl"
        "filter_layer_node_offset.cu"
        "find_perfect_split.cu.inl"
        "find_perfect_split.cu"
        "generate_initial_event.cu.inl"
        "generate_initial_event.cu"
        "merge_event.cu.inl"
        "merge_event.cu"
        "populate_leaf_node_triangle_range.cu.inl"
        "populate_leaf_node_triangle_range.cu"
        "populate_node_parent.cu.inl"
        "populate_node_parent.cu"
        "resize_node.cu.inl"
        "resize_node.cu"
        "select_node_best_split.cu.inl"
        "select_node_best_split.cu"
        "separate_splitted_polygon.cu.inl"
        "separate_splitted_polygon.cu"
        "set_node_count.cu.inl"
        "set_node_count.cu"
        "split_node.cu.inl"
        "split_node.cu"
        "split_polygon.cu.inl"
        "split_polygon.cu"
        "update_polygon_node.cu.inl"
        "update_polygon_node.cu"
        "update_splitted_polygon_count.cu.inl"
        "update_splitted_polygon_count.cu"
        "update_splitted_polygon_node.cu.inl"
        "update_splitted_polygon_node.cu"
    PUBLIC_LINKS
        Thrust::Thrust
)

if(NOT CMAKE_UNITY_BUILD_BATCH_SIZE STREQUAL 0)
    set_target_properties(
        "lib${PROJECT_NAME}"
        PROPERTIES
            UNITY_BUILD_BATCH_SIZE 2
    )
endif()

if(SAH_KD_TREE_BUILD_FUZZER)
    target_compile_options(
        "lib${PROJECT_NAME}"
        PRIVATE
            -fsanitize=fuzzer-no-link
    )
    target_link_options(
        "lib${PROJECT_NAME}"
        PRIVATE
            -fsanitize=fuzzer-no-link
    )
    string(REPLACE "." ";" CXX_COMPILER_VERSION_LIST ${CMAKE_CXX_COMPILER_VERSION})
    list(GET CXX_COMPILER_VERSION_LIST 0 CXX_COMPILER_VERSION_MAJOR)
    target_link_directories(
        "lib${PROJECT_NAME}"
        PUBLIC
            "/usr/lib/clang/${CXX_COMPILER_VERSION_MAJOR}/lib/linux/"
    )
    target_link_libraries(
        "lib${PROJECT_NAME}"
        PUBLIC
            "clang_rt.fuzzer_no_main-${CMAKE_SYSTEM_PROCESSOR}"
    )
endif()
