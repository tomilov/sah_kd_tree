#pragma once

#include <../SPIRV-Reflect/spirv_reflect.h>

#include <codegen/codegen_spirv_export.h>

namespace codegen::spv
{

{% for spv_enum in spv_enums %}
[[nodiscard]] const char * toString({{ spv_enum.enum_typename }} {{ spv_enum.variable_name }}) CODEGEN_SPIRV_EXPORT;
{% endfor %}

}  // namespace codegen::spv
